import { ListeningLayoutShell } from "@/components/listening/ListeningLayoutShell";

export default function ListeningLayout({ children }: { children: React.ReactNode }) {
  return <ListeningLayoutShell>{children}</ListeningLayoutShell>;
}
